#include "bits/stdc++.h"

typedef long long ll;
using namespace std;

int main(){
  ios::sync_with_stdio(0); cin.tie(0); 

  ll numberOfTestCase;
  cin >> numberOfTestCase;

  while(numberOfTestCase--) {
    ll firstNumber;
    cin >> firstNumber >> firstNumber;

    if ( firstNumber==1){
      cout << "YES\n";
    } else {
      cout << "NO\n";
    }
    string foo;
    getline(cin,foo);

  }

  return EXIT_SUCCESS;
}